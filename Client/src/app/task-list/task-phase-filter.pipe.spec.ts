import { TaskPhaseFilterPipe } from './task-phase-filter.pipe';

describe('TaskPhaseFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskPhaseFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
